# 통합 점검 결과 — 2026-07-06

> `scripts/integration-check.js` 125 passed / 0 failed  
> 라이브 서버 없이 모듈·데이터 직접 로드 방식으로 검증

---

## 1. 인증 보완 내역

### 변경 파일

| 파일 | 변경 내용 |
|------|-----------|
| `server.js` | `adminAuth()`: `|| 'englab2024'` 하드코딩 제거 → ADMIN_PASSWORD 미설정 시 503 반환 |
| `server.js` | 기동 시 `ADMIN_PASSWORD`, `TOKEN_SECRET` 환경변수 미설정 경고 출력 |
| `server.js` | `GET /api/assignment/:code/report` (학생용): `assessment.speaking.sttText` 제거 후 응답 |
| `lib/assign-store.js` | `loginAcademy()` fallback: `|| 'englab2024'` 제거 → 환경변수 없으면 예외 |

### 변경하지 않은 것 (이유)

| 항목 | 판단 |
|------|------|
| `?pw=` URL 파라미터 방식 (legacy admin) | `/admin` 페이지는 내부 관리용. UX 개편 = 큰 리팩터링 → 배포 전 리스크 최소화 원칙 준수 |
| `hashPw` 고정 salt `'englab2024:'` | sha256 + 고정 salt는 보안상 취약하지만 현 PoC 범위에서는 허용. 운영 전환 시 bcrypt/argon2로 교체 권장 |
| `TOKEN_SECRET` 기본값 (`englab-token-secret-2024`) | 하드코딩 제거 시 기존 원장 토큰 전수 무효화됨. 기동 경고 추가로 최소 보완 |

---

## 2. 통합 점검 PASS 항목 (125개)

### Step 1 — 원장 로그인 · 학생 생성 · 배정 (23)
- 학원 로그인 → HMAC 토큰 발급 · 토큰 검증 성공
- 위조 토큰 → null 반환
- 틀린 비밀번호 → 예외
- 이메일 없는 학생 생성 → 예외
- 동일 학원 내 중복 이메일 → 예외
- Stage 1(Seeker) 비활성 → 배정 예외
- Stage 2~5 각 accessCode 발급(8자 hex) · status=assigned · academyId 바인딩

### Step 2 — composeTest · 문항 구조 · D&D · 스피킹 (49)
- Stage 1 active:false, questions 빈 배열
- Stage 2~5 각: active:true · 30문항 · base 비율 70%(60~80% 허용) · 독해 지문 온전성 · vocabulary/grammar 존재 · 모든 문항 id 존재
- unscramble 정오판정: 정답·대소문자 무시·오답·빈 제출
- extractTrailingPunct 구두점 추출
- questions.speaking.json 로드(16문항) · anyResponseValid 동작
- buildSpeakingReport: responseRate 0~100 · sttText 존재 · 무응답=0 · 0문항=null

### Step 3 — 제출 · 채점 · status=done (23)
- 시험 시작 → in_progress · quiz 저장
- scoreAndDiagnose: score/total/scorePct · appropriate(level·cefr·recommended) · challenge(signal·upperCorrectRate) · sections(4개 영역) 존재
- **speaking.responseRate 초기값 null** — 레벨 산출 완전 분리 확인
- 스피킹 덮어쓰기(server.js 로직 시뮬레이션): responseRate/sttText 반영
- submitAssignment → done · completedAt 기록 · 중복 제출 예외

### Step 4 — 원장 리포트 (12)
- quiz(정답) 필드 제외 확인
- assessment 내 sections·appropriate·challenge·speaking 존재
- speaking.responseRate · sttText 모두 포함 (원장 전용)
- 경쟁사 비교표 파일 존재 · assessment와 완전 분리(별도 API)

### Step 5 — 학생 리포트 보안 (9)
- studentName·stage·completedAt·assessment 포함
- `sttText` 제거됨 · responseRate는 포함
- 학생 리포트 JSON에 경쟁사 데이터 없음
- server.js 소스 내 sttText 제거 로직 존재 확인

### Step 6 — Academy 격리 (9)
- 학원A/B 배정 목록 교차 없음
- 각 학원 배정·학생 모두 자기 academyId로만 필터링
- 학원B 토큰으로 학원A 배정 조회 → 빈 결과
- ADMIN_PASSWORD 미설정 + 해시 없는 학원 → 로그인 예외(서버 설정 오류)

---

## 3. 배포 전 사람이 직접 확인해야 할 항목

### 🔴 High — 배포 전 필수

| # | 항목 | 이유 |
|---|------|------|
| H-1 | **`.env.local`에 `ADMIN_PASSWORD` 설정** | 미설정 시 `/api/admin/*` 전체 503. 기본값 제거됨. |
| H-2 | **`.env.local`에 `TOKEN_SECRET` 설정** | 미설정 시 기본값(`englab-token-secret-2024`) 사용 → 토큰 위조 가능. |
| H-3 | **`/admin` 페이지 접근 제한** | `?pw=` URL 파라미터 방식은 서버 로그·브라우저 히스토리에 비밀번호 노출. 내부 망 또는 IP 화이트리스트 권장. |
| H-4 | **Railway 환경변수 등록** | `ADMIN_PASSWORD`, `TOKEN_SECRET`, `EMAIL_USER`, `EMAIL_PASS`, `EMAIL_FROM`, `TEACHER_EMAIL`, `ANTHROPIC_API_KEY` — 코드에 없고 Railway 대시보드에만 존재해야 함. |

### 🟡 Medium — 운영 전환 시 권장

| # | 항목 | 이유 |
|---|------|------|
| M-1 | **`diagnostics.json` 파일 준비** | 현재 Challenger 레벨 기준으로만 `appropriate.recommended` 값이 채워짐. Builder(stage 2)는 `diagnostics.builder.json` 필요. 없으면 recommended=null로 리포트에 빈 칸. |
| M-2 | **`data/competitor-map.json` 실제 데이터로 교체** | 현재 예시 데이터("예시 레벨 A", "예시 레벨 B", "예시 레벨 C"). 본사가 실제 경쟁사 레벨로 교체 필요. |
| M-3 | **Stage 3~5 `ourLevel`을 competitor-map에 추가** | 현재 "Challenger 3", "Challenger 6" 2개 행만 있음. Stage 4(Explorer), 5(Inventor)의 `appropriate.recommended` 값에 해당하는 행 추가 필요. |
| M-4 | **학생 리포트 화면에서 sttText 미표시 확인 (브라우저 수동)** | server.js에서 제거 로직 추가됨. `/test` 페이지 결과 화면에서 STT 텍스트가 보이지 않는지 실제 브라우저로 확인. |

### 🟢 Low — 나중에 검토

| # | 항목 | 이유 |
|---|------|------|
| L-1 | `hashPw` 고정 salt → bcrypt/argon2 교체 | 현재 sha256 + 고정 salt. PoC 수준은 OK, 운영 전환 시 교체 권장. |
| L-2 | `sessionStorage` 토큰 → HttpOnly 쿠키 전환 | XSS 발생 시 토큰 탈취 가능. PoC 범위 밖. |
| L-3 | `/api/assignment/:code/report` 공개 endpoint | 코드를 알면 누구나 조회 가능. 운영 시 토큰 인증 추가 또는 TTL 적용 검토. |
| L-4 | Stage 1(Seeker) 문항 이미지 준비 | 현재 비활성. 이미지 수급 후 활성화 시 `isStageActive` 자동 인식. |

---

## 4. 기존 검증 스크립트 최종 상태

| 스크립트 | 결과 |
|----------|------|
| `scripts/writing-dnd-check.js` | 37/37 PASS |
| `scripts/speaking-check.js` | 51/51 PASS |
| `scripts/competitor-map-check.js` | 38/38 PASS |
| `scripts/integration-check.js` | 125/125 PASS |

---

## 5. 아키텍처 정합성 확인 결과

```
학생(test.html)
  └─ GET /api/assignment/:code         → 이름·단계만 (공개)
  └─ GET /api/assignment/:code/questions → 문항 정답 제거 후 반환
  └─ POST /api/assignment/:code/submit  → 채점 + 이메일
  └─ GET /api/assignment/:code/report   → assessment (sttText 제거됨) ✓

원장(admin-assign.html)
  └─ POST /api/admin/login              → HMAC 토큰 발급
  └─ GET /api/admin/students            → 학원 스코프 격리 ✓
  └─ POST /api/admin/assignments        → 학원 스코프 강제 ✓
  └─ GET /api/admin/assignments/:code/report → quiz 제외, sttText 포함 ✓
  └─ GET /api/competitor-map            → requireAcademy 인증 필수 ✓

글로벌 관리자(admin.html)
  └─ GET /api/admin/results             → ?pw= ADMIN_PASSWORD 필수 (하드코딩 제거됨) ✓
  └─ GET /api/admin/logs                → 동일
```

**academy 격리**: 모든 `requireAcademy` 라우트는 토큰의 `academyId`로 데이터 필터링. 크로스-아카데미 접근 불가 확인.

**스피킹 레벨 미반영**: `scoreAndDiagnose` 반환 시 `speaking: {responseRate:null, sttText:null}`. 서버가 이후 `SU.buildSpeakingReport()`로 덮어쓰지만 점수 계산 이후이므로 레벨 산출에 무영향.
